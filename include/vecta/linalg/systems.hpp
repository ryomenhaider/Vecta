#pragma once

#include "vecta/linalg/elimination.hpp"

namespace vecta::linalg
{
    template <typename T>
    Matrix<T> particular_solution(const Matrix<T>& A, std::size_t b){
        
    };

    template <typename T>
    Matrix<T> general_solution(const Matrix<T>& A, std::size_t b){

    };
    
} // namespace vecta::linalg
