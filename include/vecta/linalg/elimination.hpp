#pragma once

#include "vecta/linalg/core.hpp"

namespace vecta::linalg 
{
    
    template <typename T>
    Matrix<T> row_echelon_form(const Matrix<T>& A){
        
    };

    template <typename T>
    Matrix<T> reduced_row_echelon_form(const Matrix<T>& A){

    };
    
    template <typename T>
    Matrix<T> gaussian_elimination(const Matrix<T>& A,const T& b){

    };


} // namespace vecta::linalg
