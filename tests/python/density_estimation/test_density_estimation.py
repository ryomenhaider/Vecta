from vecta import density_estimation


def test_density_estimation_importable():
    # Smoke test only -- replace with real tests once you implement this chapter.
    assert hasattr(density_estimation, "placeholder")
    assert density_estimation.placeholder() == 0
