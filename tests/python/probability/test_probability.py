from vecta import probability


def test_probability_importable():
    # Smoke test only -- replace with real tests once you implement this chapter.
    assert hasattr(probability, "placeholder")
    assert probability.placeholder() == 0
