from vecta import analytic_geometry


def test_analytic_geometry_importable():
    # Smoke test only -- replace with real tests once you implement this chapter.
    assert hasattr(analytic_geometry, "placeholder")
    assert analytic_geometry.placeholder() == 0
