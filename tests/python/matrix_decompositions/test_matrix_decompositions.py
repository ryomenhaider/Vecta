from vecta import matrix_decompositions


def test_matrix_decompositions_importable():
    # Smoke test only -- replace with real tests once you implement this chapter.
    assert hasattr(matrix_decompositions, "placeholder")
    assert matrix_decompositions.placeholder() == 0
