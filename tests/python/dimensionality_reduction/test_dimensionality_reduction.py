from vecta import dimensionality_reduction


def test_dimensionality_reduction_importable():
    # Smoke test only -- replace with real tests once you implement this chapter.
    assert hasattr(dimensionality_reduction, "placeholder")
    assert dimensionality_reduction.placeholder() == 0
